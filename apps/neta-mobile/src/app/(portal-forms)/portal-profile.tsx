import { useEffect, useState } from 'react'; import { StyleSheet, Text } from 'react-native'; import { router } from 'expo-router'; import { FormSheet, useKeyboardForm } from '@/components/forms'; import { InfoBox, TextField, useToast } from '@/components/ui'; import { getPortalProfile, updatePortalProfile } from '@/features/portal/api'; import { toClientError } from '@/lib/api/errors'; import { useSession } from '@/providers/session-provider'; import { useTheme } from '@/providers/theme-provider';
type Field = 'name'; export default function PortalProfileFormRoute() { const session = useSession(); const { colors } = useTheme(); const { showToast } = useToast(); const keyboard = useKeyboardForm<Field>(); const [name, setName] = useState(''); const [baseline, setBaseline] = useState(''); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(true); const [saved, setSaved] = useState(false); useEffect(() => { if (saved) router.back(); }, [saved]); useEffect(() => { if (session.status !== 'authenticated' || session.role !== 'client') return; let active = true; void getPortalProfile(session.instance, session.user).then((result) => { if (active) { setName(result.data.name); setBaseline(result.data.name); } }).catch((value) => active && setError(toClientError(value, 'Profil alınamadı.').message)).finally(() => active && setLoading(false)); return () => { active = false; }; }, [session]); const submit = async () => { if (session.status !== 'authenticated' || session.role !== 'client') return; if (!name.trim()) { keyboard.focusFirstError({ name: 'error' }, ['name']); return; } setLoading(true); try { await updatePortalProfile(session.instance, session.user, { name: name.trim() }); await session.refreshSession(); setSaved(true); showToast({ message: 'Portal profil bilgilerin güncellendi.', tone: 'success' }); } catch (value) { setError(toClientError(value, 'Profil güncellenemedi.').message); } finally { setLoading(false); } }; return <FormSheet dirty={!saved && !loading && name !== baseline} onSubmit={() => void submit()} contentRef={keyboard.contentRef} onViewportLayout={keyboard.onViewportLayout} scrollRef={keyboard.scrollRef} submitting={loading} title="Profili düzenle"><Text style={[styles.lead, { color: colors.textMuted }]}>Portal ve proje ekranlarında görünecek adını düzenle.</Text>{error ? <InfoBox description={error} title="Kaydedilemedi" tone="danger" /> : null}<TextField error={!name.trim() ? 'Görünen ad zorunludur.' : undefined} label="Görünen ad" onChangeText={setName} onFocus={() => keyboard.onFocus('name')} ref={keyboard.register('name')} value={name} /></FormSheet>; } const styles = StyleSheet.create({ lead: { fontSize: 15, lineHeight: 22 } });
