import { useEffect, useMemo, useState } from 'react'; import { router } from 'expo-router'; import { FormSheet, useKeyboardForm } from '@/components/forms'; import { InfoBox, TextField, useToast } from '@/components/ui'; import { changePassword } from '@/features/settings/api'; import { toClientError } from '@/lib/api/errors'; import { useSession } from '@/providers/session-provider';
type Field = 'current' | 'next'; export default function PortalSecurityFormRoute() { const session = useSession(); const { showToast } = useToast(); const keyboard = useKeyboardForm<Field>(); const initial = useMemo(() => ({ current: '', next: '' }), []); const [form, setForm] = useState(initial); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); const [saved, setSaved] = useState(false); useEffect(() => { if (saved) router.back(); }, [saved]); const submit = async () => { if (session.status !== 'authenticated' || session.role !== 'client') return; if (!form.current || form.next.length < 8) { keyboard.focusFirstError({ current: !form.current ? 'error' : undefined, next: form.next.length < 8 ? 'error' : undefined }, ['current', 'next']); setError('Mevcut şifreyi ve en az 8 karakterli yeni şifreyi gir.'); return; } setLoading(true); try { await changePassword(session.instance, session.user, { currentPassword: form.current, newPassword: form.next, revokeOtherSessions: true }); setSaved(true); showToast({ message: 'Şifren güncellendi; diğer oturumlar kapatıldı.', tone: 'success' }); } catch (value) { setError(toClientError(value, 'Şifre güncellenemedi.').message); } finally { setLoading(false); } }; return <FormSheet dirty={!saved && JSON.stringify(form) !== JSON.stringify(initial)} onSubmit={() => void submit()} contentRef={keyboard.contentRef} onViewportLayout={keyboard.onViewportLayout} scrollRef={keyboard.scrollRef} submitLabel="Şifreyi değiştir" submitting={loading} title="Güvenlik">{error ? <InfoBox description={error} title="Şifre değiştirilemedi" tone="danger" /> : null}<TextField autoComplete="current-password" label="Mevcut şifre" onChangeText={(current) => setForm((value) => ({ ...value, current }))} onFocus={() => keyboard.onFocus('current')} ref={keyboard.register('current')} secureTextEntry value={form.current} /><TextField autoComplete="new-password" label="Yeni şifre" onChangeText={(next) => setForm((value) => ({ ...value, next }))} onFocus={() => keyboard.onFocus('next')} ref={keyboard.register('next')} secureTextEntry value={form.next} /></FormSheet>; }
